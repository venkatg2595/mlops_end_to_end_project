# mlops_end_to_end_project
End to end project development

1. Project introduction
2. github repo setup
3. project template
4. reuirements installation ---> conda, python, and all reuiremtns from the reuirements.txt file

TO create virtual env use elow command
''' bash
conda create -n mlproject python=3.13 -y
'''

to activatge env 
'''bash
conda activate mlproject
'''

to install reuirements
to activatge env 
'''bash
pip install requirements.txt
'''

Id CommandLine
  -- -----------
   1 try { . "c:\Users\Admin\AppData\Local\Programs\Microsoft VS Code\resources\app\out\vs\workbench\contrib\terminal\co...
   2 conda create -n mlproject python=3.13 -y
   3 conda activate mlproject
   4 conda inti
   5 conda init
   6 conda activate mlproject
   7 conda init
   8 conda activate mlproject
   9 conda activate
  10 conda init
  11 cd C:\Users\Admin\anaconda3\Scripts
  12 .\activate
  13 conda activate mlproject
  14 .\activate.bat
  15 D:
  16 pip install .\requirements.txt
  17 pip install -r .\requirements.txt
  18 pip install -r .\requirements.txt
  19 pip install scikit-learn
  20 pip install -r .\requirements.txt
  21 pip install -r .\requirements.txt
  22 python main.py
  23 python main.py
  24 python main.py
  25 python main.py
  26 git ranch
  27 git branch
  28 git add .
  29 git status
  30 git commit -m "created and updated requirements.txt and tested with main.py for sample"
  31 git push origin dev
  32 ls
  33 clear

1. noteook experiment
2. project utilitie ---> logging, exceptions, utiles model
3. project workflows

# noteook experiment:
1. load and read the data
2. trian the model
3. predic the model
4. evalute the model using metric

# Logging:
to generate log formate and and export logs into log folder for that need to use logging

# Utiles:

Rather than using muliple folders of commons files just need to mention at common file and call them to where ever is required.

# Workflow:
1. update config.yaml
2. update schema.yaml
3. update params.yaml 
4. update the entity
5. update the configuration manager in src config
6. update the components
7. update the pipeline
8. update the main.py
9. update the app.py


# As of now covered 
1. Project introduction
2. Git repo setup
3. Project template
4. Requirements installation

5. Project utils
    1. logging
    2. exceptions
    3. utils
6. Notebook experiment
7. project workflows
